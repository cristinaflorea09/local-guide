{
  "name": "stripe-seller-subscriptions",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^7.0.3",
    "stripe": "^14.0.0"
  },
  "scripts": {
    "lint": "echo \"skip lint\"",
    "build": "echo \"skip build\"",
    "deploy": "firebase deploy --only functions"
  }
}
